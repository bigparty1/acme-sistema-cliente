class TableActionButtonModel {
  constructor({label, action, tooltip, icon, style}) {
    this.label = label;
    this.action = action;
    this.tooltip = tooltip;
    this.icon = icon;
    this.style = style;
  }
}

export default TableActionButtonModel;